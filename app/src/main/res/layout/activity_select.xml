<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/bg_start">

    <android.support.v4.view.ViewPager
        android:id="@+id/viewPager_select"
        android:layout_width="wrap_content"
        android:layout_height="356dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dp">
    </android.support.v4.view.ViewPager>

    <me.relex.circleindicator.CircleIndicator xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/guild_circleIndicator"
        android:layout_width="wrap_content"
        android:layout_height="24dp"
        android:layout_below="@+id/viewPager_select"
        android:layout_centerHorizontal="true"
        app:ci_drawable="@drawable/bg_guild_indicator_selected"
        app:ci_drawable_unselected="@drawable/bg_indicator_normal"
        app:ci_height="4dp"
        app:ci_width="4dp" />

    <TextView
        android:id="@+id/iv_guide_enter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:background="@mipmap/bg_button"
        android:contentDescription="@null"
        android:gravity="center"
        android:text="@string/select_use"
        android:textColor="@color/white"
        android:visibility="visible" />
</RelativeLayout>